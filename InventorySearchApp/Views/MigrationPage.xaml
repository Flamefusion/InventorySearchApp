
<Page
    x:Class="InventorySearchApp.Views.MigrationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:InventorySearchApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <StackPanel Margin="12" Spacing="12">
            <Button Content="Select Credentials File" Command="{x:Bind ViewModel.PickFileCommand}"/>
            <TextBlock Text="{x:Bind ViewModel.SelectedFileName, Mode=OneWay}"/>
            <Button Content="Start Migration" Command="{x:Bind ViewModel.StartMigrationCommand}"/>
        </StackPanel>
    </Grid>
</Page>
