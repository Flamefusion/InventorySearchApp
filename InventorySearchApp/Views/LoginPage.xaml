<Page
    x:Class="InventorySearchApp.Views.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:InventorySearchApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="12">
            <TextBox Header="Email" Text="{x:Bind ViewModel.Email, Mode=TwoWay}" Width="300"/>
            <PasswordBox Header="Password" Password="{x:Bind ViewModel.Password, Mode=TwoWay}" Width="300"/>
            <Button Content="Login" Command="{x:Bind ViewModel.LoginCommand}" HorizontalAlignment="Center"/>
            <TextBlock Text="{x:Bind ViewModel.ErrorMessage}" Foreground="Red" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Page>
