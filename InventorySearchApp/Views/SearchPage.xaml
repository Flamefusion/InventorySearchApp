
<Page
    x:Class="InventorySearchApp.Views.SearchPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:InventorySearchApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid RowDefinitions="Auto, *">
        <StackPanel Grid.Row="0" Margin="12">
            <TextBox Header="Serial Numbers (one per line)" AcceptsReturn="True" Height="150" Text="{x:Bind ViewModel.SerialNumbers, Mode=TwoWay}" Margin="0,12,0,0"/>
            <Button Content="Search" Margin="0,12,0,0" Command="{x:Bind ViewModel.SearchCommand}"/>
        </StackPanel>

        <ListView Grid.Row="1" Margin="12,0,12,12" ItemsSource="{x:Bind ViewModel.SearchResults}">
            <ListView.HeaderTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Serial Number" Style="{ThemeResource BaseTextBlockStyle}"/>
                        <TextBlock Grid.Column="1" Text="Box" Style="{ThemeResource BaseTextBlockStyle}"/>
                    </Grid>
                </DataTemplate>
            </ListView.HeaderTemplate>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding Serial}"/>
                        <TextBlock Grid.Column="1" Text="{Binding Box}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
